<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Search Films</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="style/main.css" />
    <link rel="stylesheet" type="text/css" media="screen" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" />
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <script type="text/javascript" src="script/main.js"></script>
</head>
<body>
    <div class="container">
        <div class="search-component">
            <form method="POST">
                <div id="get-genre" class="form-inline">
                    <input name="genre" oninput="handleChangeInput(this)" class="form-control" placeholder="Enter your favorite genre ..." />
                    <button type="button" class="btn btn-primary disable">NEXT</button>
                </div>
                <div id="get-actor" class="form-inline hidden">
                    <input name="actor" oninput="handleChangeInput(this)" class="form-control" placeholder="Enter your favorite actor ..." />
                    <button type="submit" class="btn btn-primary disable">SEARCH</button>
                </div>
            </form>
        </div>
        <div class="row">
            <div class="list-movie col-md-6">
                {% for movie in movies|slice(0, 4) %}
                    <div class="movie-item row">
                        <div class="col-md-3">
                            <img src="{{ movie.Img }}" class="img-thumbnail">
                        </div>
                        <div class="col-md-9">
                            <h4><a target="blank" href="{{ movie.ImdbLink }}">{{ movie.Title }}</a> ({{ movie.Rating }}/10)</h4>
                            <p class="genre">Genre: {{ movie.getGenre() }}</p>
                            <p class="actor">Main actor: {{movie.getMainActor()}}</p>
                            <p class="synopsis">Synopsis: {{ movie.Synopsis }}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="list-movie col-md-6">
                {% for movie in movies|slice(4, 4) %}
                    <div class="movie-item row">
                        <div class="col-md-3">
                            <img src="{{ movie.Img }}" class="img-thumbnail">
                        </div>
                        <div class="col-md-9">
                            <h4><a target="blank" href="{{ movie.ImdbLink }}">{{ movie.Title }}</a> ({{ movie.Rating }}/10)</h4>
                            <p class="genre">Genre: {{ movie.getGenre() }}</p>
                            <p class="actor">Main actor: {{movie.getMainActor()}}</p>
                            <p class="synopsis">Synopsis: {{ movie.Synopsis }}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
</body>
</html>